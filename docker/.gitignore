# ── Secrets -- never commit ───────────────────────────────────────────────────
.env

# ── TAK Server RPM (must be downloaded from tak.gov — NOT included in repo) ──
# Requires a TAK.gov account with MFA. Copy the RPM here after downloading.
# See Documentation/Docker-Deploy-Runbook.md § 2 — Obtain the TAK Server RPM.
takserver-*.noarch.rpm
takserver-*.rpm
takserver-public-gpg.key

# ── Generated test artefacts -- never commit ─────────────────────────────────
*.p12
*.jks
*.pem
*.log
